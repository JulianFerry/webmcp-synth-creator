import fixture from '../../../fixtures/patches/warm-mono-bass.patch.json' with { type: 'json' }
import { createCuratedPatch } from './shared'

export const WARM_MONO_BASS_PATCH = createCuratedPatch(fixture)
