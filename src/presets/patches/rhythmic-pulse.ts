import fixture from '../../../fixtures/patches/rhythmic-pulse.patch.json' with { type: 'json' }
import { createCuratedPatch } from './shared'

export const RHYTHMIC_PULSE_PATCH = createCuratedPatch(fixture)
